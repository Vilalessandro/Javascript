function soma(n1=0, n2=0)/*O numero 0 depois do n1, serve para quando tiver só um numero na soma, ele não retornar undefined, retorna somente o numero qu existe*/{
    return n1 + n2
}

console.log(soma(5, 8))